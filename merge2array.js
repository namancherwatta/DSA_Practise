let arr1=[1,4,5,8]
let arr2=[2,3,6]

function merge(arr1,arr2){
        let m=arr.length
        let n=arr.length
    let j=Math.max(n,m)
for(let i=0;i<j;i++){
      if(arr1[i] || arr[j]){
       let min=Math.min(arr1[i],arr2[i])
      }

}



}

console.log(merge(arr1,arr2))
